<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Handle Angular and Vue.js HTML5 mode / React Router
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]
</IfModule>